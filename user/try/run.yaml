apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: user-run-images
spec:
  serviceAccountName: sock-shop
  taskRef:
    name: user
  resources:
    inputs:
    - name: user-p-res
      resourceRef:
        name: user-p-res
  params:
    - name: IMAGE
      value: "rayanah/user"
