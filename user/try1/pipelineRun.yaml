apiVersion: tekton.dev/v1alpha1
kind: PipelineRun
metadata:
  name: user-run
spec:
  serviceAccountName: sock-shop
  pipelineRef:
    name: user-pipeline
  resources:
    - name: user-pr
      resourceRef:
        name: user-pr
