apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: carts-deploy-kubectl
  serviceAccountName: carts-deploy
spec:
  params:
    - name: pathToYamlFile
      type: string
      description: The path to the yaml file to deploy within the git source
      default: $(resources.inputs.carts-pr.path)/cart/cart-dep-ser.yaml
    - name: pathToDbYamlFile
      type: string
      default:  $(resources.inputs.carts-pr.path)/cart/cartdb-dep-ser.yaml 
    - name: namespace
      default: test
  resources:
    inputs:
      - name: carts-pr
        type: git
  steps:
   - name: run-kubectl
     image: lachlanevenson/k8s-kubectl
     command: ["kubectl"]
     args:
       - "apply"
       - "-f"
       - "$(params.pathToDbYamlFile)"
       - "-f"
       - "$(params.pathToYamlFile)"
       - "-n"
       - "$(params.namespace)"
   - name: sleep
     image: ubuntu
     args: ['-c', 'sleep 90']

